// Hooks
import { makeStyles } from '@hooks/theme';

const styles = makeStyles(() => ({
  textContainer: {
    marginHorizontal: 24,
    marginVertical: 12,
  },
  title: {
    fontSize: 22,
  },
  buttonContainer: {
    height: 64,
    paddingVertical: 8,
  },
}));

export default styles;
