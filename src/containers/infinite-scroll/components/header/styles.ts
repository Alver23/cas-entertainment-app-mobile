// Hooks
import { makeStyles } from '@hooks/theme';

const styles = makeStyles(() => ({
  loading: {
    margin: 10,
  },
  container: {
    alignItems: 'center',
    marginVertical: 8,
  },
}));

export default styles;
